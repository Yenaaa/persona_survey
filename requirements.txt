# LLM_PROVIDER로 고른 provider에 맞는 패키지만 설치하면 됨 (기본: openai)
openai>=1.40.0
# anthropic>=0.40.0      # LLM_PROVIDER=anthropic 일 때
# google-genai>=0.3.0    # LLM_PROVIDER=google 일 때

datasets>=2.19.0
pydantic>=2.7.0
python-dotenv>=1.0.0
